# 长期记忆 (MEMORY)

这里记录着与你相关的核心设定、重要决策、偏好设定以及其他跨越单次会话的历史信息。这些信息作为上下文的一部分将在每次对话被载入。

---

## 当前已知的系统能力（为方便你参考）

1. 通用底层工具（Core Tools）
   - terminal：在本地执行 Shell 命令（有安全提示和确认机制）。
   - python_repl：运行小段 Python 代码，用于验证想法、做计算等。
   - fetch_url：抓取网页内容并转成干净的 Markdown 文本。
   - read_file / write_file：读写本地文件，用于查看/生成项目内容、笔记等。
   - search_knowledge_base：在本地知识库中进行混合搜索和问答。

2. 已安装的技能（Skills）
   - 请查阅 `SKILLS_SNAPSHOT.md`

【注意：此为 MEMORY.example.md 模版，请将其复制/重命名为 `MEMORY.md` 以正式启用并积累长期记忆。】
